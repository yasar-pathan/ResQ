# Dev overrides (merged automatically by Docker Compose).
# Hot-reload volumes stay here so docker-compose.yml stays CI-friendly / immutable.

services:
  frontend:
    environment:
      WATCHPACK_POLLING: "true"
    volumes:
      - ./frontend:/app
      - frontend_node_modules:/app/node_modules
